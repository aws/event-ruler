### Issue #, if available:

### Description of changes:

<Add details here. Mention why you are making this change and any related documents/links/artifacts. If you make any source code changes, also mention the testing and performance.>

#### Dependencies (if the change adds one, upgrades one, or changes the scope of one):

<For an added dependency, or a scope change that makes one reach consumers (test to compile, or removing `<optional>`), answer the questions the [Dependencies](https://github.com/aws/event-ruler/blob/main/CONTRIBUTING.md#dependencies) section of CONTRIBUTING.md asks for the dependency's scope: why not write it, who benefits, the measured benefit and cost, health, license. For an upgrade: patch, minor, or major, and the `scripts/perf-compare.sh` result where that section asks for one. For a scope change in the other direction: the reason.>

#### Benchmark / Performance (for source code changes):

```
<replace this with output from /src/test/software/amazon/event/ruler/Bechmarks.java here.

The benchmark results can be fetched from "Pull request checks -> Java build -> build (ubuntu-X.Y, 8) -> Run benchmarks".>
```

---

By submitting this pull request, I confirm that my contribution is made under the terms of the Apache 2.0 license.
